{
  "name": "studio-gestion",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix back\" \"npm run dev --prefix front\""
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}
